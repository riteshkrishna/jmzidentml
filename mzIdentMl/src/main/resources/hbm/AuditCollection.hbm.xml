<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
    <class name="uk.ac.ebi.jmzidml.model.mzidml.AuditCollection" table="AuditCollection" >
        <id name="hid" access="field" type="java.lang.Long">
            <generator class="org.hibernate.id.enhanced.SequenceStyleGenerator" />
        </id>

    <!-- <list name="contactGroup" table="AuditCollection_Contact" cascade="all" access="field">
            <key column="AuditCollection_hid"/>
            <index column="Contact_idx"/> -->
        <!-- this does not cause an exception due to non-existent contact table. This is because new join table
        is created. The hid space is shared between tables of child classes so hibernate can tell which instance to create
         -->
<!--            <many-to-many column="Contact_hid" class="uk.ac.ebi.jmzidml.model.mzidml.Contact"/> -->


   <!--
        <one-to-many   class="uk.ac.ebi.jmzidml.model.mzidml.Contact" />
        
    </list> -->
    </class>
</hibernate-mapping>
